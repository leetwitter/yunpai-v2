# 实现偏差记录（相对书一/书二 v1.0 基线）

> 代码阶段实际实现与书二设计的偏差，逐条记录供审核；原则性偏差需回写书二后再冻结 v1.1。

| # | 偏差 | 书二位置 | 实际实现 | 原因 |
|---|---|---|---|---|
| 1 | 包布局 | §1.1 深层子包（storage/ recognition/ tools/…） | A 级随迁资产**平铺于包根**（文件名与旧包一致），新模块在 orchestrator/ worker/ reviewer/ api/ 子包 | 最小化 30+ 文件的 import 改写面，降低随迁破坏面 |
| 2 | manifest 基线 | §7.3 基于 115（registry/tool-manifests） | 沿用 115 + local.json（识别 4 件套合同）= 119；旧打包目录（91）与 fin-m6 版（m0=32）**均不采信** | 双目录漂移治理：单目录 registry-manifests/ 为唯一事实源；fin-m6 增量待合流 main 后走注册吸收 |
| 3 | API 路径 | §8 写 /api/runs 系列 | 沿旧实现真实路径 **/runs** 系列（legacy api.py 实测） | 契约兼容以旧实现为准（前端对接事实） |
| 4 | 技能文档目录 | skills-docs/ | **skills/** | 随迁测试（SKILLS_DIR）与生态一致性 |
| 5 | 执行模型 | §3.3 AsyncSqliteSaver 直配 | **专属后台事件循环线程** + AsyncSqliteSaver（在该循环内构造），端点经 run_coroutine_threadsafe | AsyncSqliteSaver 绑定构造时循环，TestClient 每请求新循环会崩；同步 SqliteSaver 又不支持 async 图（NotImplementedError）——实测排除后定型 |
| 6 | Python 基线 | §1.2 >=3.11 | venv 锁 **Python 3.12**（.venv/） | langgraph 1.2.9 在 3.10 async 上下文 get_config 失效（interrupt 不可用，实测） |
| 7 | workflows 唯一位置 | 目录树根 workflows/ | **包内** src/yunpai_orchestrator/workflows/（workflow_registry 按 `__file__` 定位） | 消灭双副本；根目录版已并入 |
| 8 | Gate 挂起可见性 | §3.4"state 由 checkpointer 持久化" | 补充：reviewer 在 interrupt() 前 **side-effect 落库** waiting_human+pending_gate 到 runs 表 | interrupt 暂停点节点未返回、checkpointer 状态不含 pending_gate；GET /runs/{id} 与 resume 依赖 runs 表镜像 |
| 9 | 审查规则覆盖 | §6.1 全集迁移对照 | 规则表覆盖主干 6 工具 + BLOCKED_INPUT + 合同默认门；**逐条 parity 对照在 M3 R2-R5 注册批次台账完成** | 骨架期先锁语义主干，全集对照随注册批次逐工具核 |
| 10 | LLM 默认值 | §1.3 | llm.py from_env 默认统一 8088/27b（与 config 同源，test_config 锁定） | 治痛点 9 落地 |
| 11 | 依赖补充 | §1.2 | 增 python-multipart / aiosqlite / python-docx / pypdf | 上传端点、AsyncSaver、识别/文档解析实测需要 |
| 12 | 批次划分勘误（D-004，用户确认） | 书一 §12.3/§13.1、书二 §7.3/§14 | 原六批总数账 106≠129（M0 余 23 无批）；修订为**七批**：R2 后增补 R3=M0 余 23，原 R3-R6 顺延为 R4-R7（9+24+23+20+26+17+10=129）；书一/书二已同步回写 | 两书批次口径自相矛盾，docs/07 §2 提请裁定，2026-09-09 用户单选确认 |
| 13 | LLM 路由 prompt 工作流目录（已修复） | §4.1 路由 prompt 全文设计：JSON 契约含 workflow_id、铁律 2「workflow_id 只能从【可用工作流】中选」、目录含 KNOWN_WORKFLOWS（id+一句话） | 实现漏注入 workflows 目录、system prompt 未要求 workflow_id、_parse_decision 丢弃该字段——三者叠加致 LLM 的 route=workflow 提案**永拒**（E2E 实测，仅靠附件确定性规则兜底）。已按书二补齐 llm.py 三处 + 7 测试（提案采纳/拒非法 id/拒缺失/拒不可见工具/prompt 目录暴露/parse 透传） | E-003 发现③；DeepSeek 真实提案复测通过（m1_m5 与 canonical_to_m5 均 source=llm） |
| 14 | Gate/存储四处接缝补全（已修复，E2E 闭环挖出） | §4.1/§5/§6.2 设计语义 | ① resume 后节点抛错会把该 resume 值固化进 checkpoint，后续 resume 不再送达（langgraph 实测最小复现）→ 非法决策改「再挂起 gate_invalid」不抛异常；② 工程门批准 approval_status 的 graph 写入点缺失（bridge.read_approved_bom 依赖的注释所指写入未实现）→ _stamp_engineering_approval + m2 fixture 补 success=true；③ AsyncSqliteSaver 与 RunRepository 同库跨线程写竞争（实测 database-is-locked 偶发丢 Gate 镜像）→ checkpoint 分库（*.checkpoint.sqlite）+ 镜像保存重试退避；④ 多行订单 m5 快照行号兜底统一 ::L1 致批内 UNIQUE 冲突 → 行号按序唯一（bridge/adapter/tools 三处）+1 测试 | WF 全链 E2E 驱动发现；修复后 m1_m5 九步闭环 completed（run-95a494af：LLM 路由+5 次 Gate 审批+排程回读），pytest 387 绿 |
| 15 | 执行链假成功三处堵漏（已修复，链路筛查） | §5.2/§6.1 设计语义 | ① 工具异常（TOOL_ERROR/UPSTREAM_UNAVAILABLE）经审查「通过」分支被静默标 completed、整链误报「业务链执行完成」（实测复现：无规则工具抛异常 → run=completed）→ 审查节点对 failed 步骤按重试余量重试、耗尽即 failed；executor 将非阻塞 success=False 也标 failed；② finalize 状态判定对 blocked/pending/running 残留误判 completed → 改为 failed > 残留(blocked) > completed；③ LLM free 空工具+空技能会空转出「0 步完成」→ _validate 拒绝空 free。共 +3 测试，pytest 390 绿 | 语义自由非事实自由的地基正确性：失败不得静默当成功 |

## 随迁测试处置台账

- 移入 `tests/legacy_rewrite/`（12+2 个，legacy_ 前缀不收集）：依赖旧 PlannerAgent/ReviewerAgent/YunpaiGraph/旧 create_app/旧双目录 manifest 政策的测试，留作 M3/M5 重写参考（README 有映射表）。
- 适配移植：test_skill_registry_consistency（SKILL_USAGE_ORDER 导入路径）、test_repository（重写为 v2 存储口径）、test_m0_contract/test_m5_lifecycle_tools（manifest 路径→registry-manifests）、test_workflow_manifest（单目录自校验）、test_evolution（observe 挂 v2 图）。
- 识别 4 件套合同（registry-manifests/local.json）按随迁测试期望补齐 required/枚举（content_b64 必填、kind 枚举、aggregate 对象）。
